/* ChatPage — standalone AI chat page (core chat is in components/AIChat.tsx) */
export default function ChatPage() {
  return (
    <div className="flex items-center justify-center h-64" dir="rtl">
      <p className="text-sm" style={{ color: 'rgba(10,12,18,0.5)' }}>
        استخدم زر المساعد الذكي في الزاوية للدردشة مع بصيرة
      </p>
    </div>
  );
}
